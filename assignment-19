Question 24: You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?
Hint 1:- Add the flag to show the different-2 data on click of buttons.
Hint 2:- Add text in the column for showing the name, phone number, and email.
Hint 3:- Add imageView to show the image.
Hint 4:- Add the image to the assets folder.
source code:
flutter:
  assets:
    - assets/images/my_image.png
import 'package:flutter/material.dart';

void main() {
  runApp(const ShowContentApp());
}

class ShowContentApp extends StatelessWidget {
  const ShowContentApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: ShowContentScreen(),
    );
  }
}

class ShowContentScreen extends StatefulWidget {
  const ShowContentScreen({super.key});

  @override
  State<ShowContentScreen> createState() => _ShowContentScreenState();
}

enum ContentToShow { none, image, details }

class _ShowContentScreenState extends State<ShowContentScreen> {
  ContentToShow _contentToShow = ContentToShow.none;

  void showImage() {
    setState(() {
      _contentToShow = ContentToShow.image;
    });
  }

  void showDetails() {
    setState(() {
      _contentToShow = ContentToShow.details;
    });
  }

  @override
  Widget build(BuildContext context) {
    Widget content;

    switch (_contentToShow) {
      case ContentToShow.image:
        content = Image.asset(
          'assets/images/my_image.png',
          width: 250,
          height: 250,
          fit: BoxFit.cover,
        );
        break;

      case ContentToShow.details:
        content = Column(
          mainAxisSize: MainAxisSize.min,
          children: const [
            Text(
              'Name: John Doe',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
            SizedBox(height: 8),
            Text(
              'Phone: +1 234 567 8900',
              style: TextStyle(fontSize: 18),
            ),
            SizedBox(height: 8),
            Text(
              'Email: john.doe@example.com',
              style: TextStyle(fontSize: 18),
            ),
          ],
        );
        break;

      default:
        content = const Text(
          'Click a button to display content',
          style: TextStyle(fontSize: 18),
        );
    }

    return Scaffold(
      appBar: AppBar(
        title: const Text('Show Image or Details'),
        backgroundColor: Colors.blueAccent,
      ),
      body: Center(
        child: content,
      ),
      bottomNavigationBar: Padding(
        padding: const EdgeInsets.symmetric(vertical: 16.0, horizontal: 24),
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceEvenly,
          children: [
            ElevatedButton(
              onPressed: showImage,
              child: const Text('Button A (Show Image)'),
            ),
            ElevatedButton(
              onPressed: showDetails,
              child: const Text('Button B (Show Details)'),
            ),
          ],
        ),
      ),
    );
  }
}
